---
name: print-readthrough-t1
description: >-
  After a 0d BMO/AMC print, write if-then first-30 tickets on 2–3 unripped
  sleeve peers. Fail the 6 PM wrap if that table is missing. Pack skill:
  agents/ssr-st/skills/print-readthrough-t1/SKILL.md. Logged miss: ORCL Thu
  AMC wrap ran 18:30 PT with no HPE/DELL card.
---

# Print read-through T+1 (desk)

Load the pack skill. On this desk, **every daily publish** must emit the table:
6:00 PM wrap, 7:00 AM FULL CHECK, Five-new NBT, 10 AM / 8 PM lessons, iMessage.
Put the best unripped peer on the iMessage `#1` when the printer beat/raise
or AH ≥ +5%. Fail the publish if the table is missing.

**Lesson (ORCL 9/10 → 9/11):** printer priced funding (capex/FCF/debt). Peers
priced the bill of materials. Ticket HPE/DELL the night of the print. Do not
add the crushed printer. Do not chase a peer already ≥ +7%.

```
If <PRINTER> beats → go <PEER> first 15–30. If miss → STAND.
WAIT FOR GO
```

Fail the wrap if a named 0d AMC/BMO has no mapped-peer rows (SKIP rows count;
silence does not). Do not arm a peer already ≥ +7%. T+2 after the peer ripped
is STAND (SNDK 6/26). No orders.
