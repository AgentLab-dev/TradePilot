# Chat — You are auditing downstream impact in the **eda-dbt-em** repo (Workday ED&A fina…

- **Agent bucket:** `arr-analyst`
- **Cursor project:** `Users-koteswararao-venkata-Documents-Cursor-eda-dbt-em`
- **Transcript id:** `subagents`
- **Last updated:** 2026-07-16T19:00:16
- **Turns kept:** 15 (user/assistant text only; tool payloads omitted)

---

## 1. user

<timestamp>Thursday, Jul 16, 2026, 6:56 PM (UTC-7)</timestamp>
<user_query>
You are auditing downstream impact in the **eda-dbt-em** repo (Workday ED&A finance dbt project) for an upstream base-layer PR.

## Background (critical, read carefully)
`eda-dbt-base` PR #491 ("fix contract mismatch") changes only the `data_type:` declarations in base-model YAML files. The base models are `select *` **passthrough** from Fivetran sources with `contract: enforced: true`. IMPORTANT: changing the YAML `data_type` does NOT physically retype any Snowflake column — the physical column type stays whatever Fivetran wrote. So downstream repos physically read the SAME bytes. The ONLY ways a downstream repo can break are:
  (a) a downstream model has `contract: enforced: true` and declares one of these columns with a type that would now conflict, OR
  (b) downstream SQL depends on the type behavior (e.g., arithmetic on a column now declared text, boolean logic, or a join/compare that assumes numeric).

## The 52 changed columns (token : transition)
number→float: forecast_value, quota_value, global_pipeline, global_acv, calc_q_4_pct_ramp, tcv_hist_convert, sales_uplift_acv_hist_convert, acv_list_hist_convert, cash_upfront_hist_convert, foreign_to_usd_currency, sku_sales_acv_hist_convert, opp_amount_hist_convert, wam, cash_upfront__c, static_pepy_eg_ratio__c, apts_subscription_relationship_rollup__c, tcv_number__c, exit_year_acv__c, financial_acv_2__c, base_price__c, sync_list_price_services__c, list_price__c, quote_tcv_decimal__c, acv_list__c, planning_acv__c, net_price__c, quote_tcv__c, sync_tcv__c, net_baseline__c, total_disc_rate_pepy__c, prism_analytics_acv__c, education_tcv__c, tcv_no_discount__c, total_list_price__c
number→text: visit_search_engine, post_search_engine, ref_type
text→float: clm_quoteacvamount__c, clm_quotetcvamount__c, opm_acvamount__c, rate__c, total_workers_by_sku__c, pro_pkg_tier_price__c, apts_sourcing_quantity__c
text→boolean: opm_populaterenewalbaselineacv__c, opm_populateacvamount__c, cmn_copadoorgrecordidmatch__c, sales_reporting_sku__c, integrate_to_workday__c
text→number: apts_reporting_history_month_price__c, apts_total_reporting_months__c, apts_accounting_net_price__c

## Your tasks
1. Get the repo on the **prod** branch: `gh repo clone workday-inc/eda-dbt-em /tmp/em_prod_audit -- --depth 1 --branch prod` (if /tmp/em_prod_audit exists and is on prod, reuse it).
2. For EVERY column token above, search ALL `.sql` and `.yml`/`.yaml` under `models/` (use ripgrep). Record each hit: file/model, and whether the reference is (i) selecting from a base/source object that maps to the changed base table, (ii) inside a `contract: enforced: true` model with a declared `data_type`, (iii) used in arithmetic / CAST / boolean logic / join.
3. Note whether eda-dbt-em even sources from eda-dbt-base's Salesforce/Google-Sheets base models, or from a separate pipeline (e.g., REDSHIFT_HISTORY). This determines exposure.
4. Cross-check physical prod types: for any consumed column, query Snowflake via the snowflake MCP tool (`user-snowflake` / `snowflake_query`) against the appropriate `*_prod` finance database `information_schema.columns` to see the current physical type.
5. If feasible without triggering an interactive SSO browser hang, attempt a compile-level e2e check (dbt parse/compile of affected models). If it would require SSO/warehouse creds you can't get non-interactively, SKIP it and say so — do NOT hang.

## Deliverable (return this exactly)
- One-line verdict: **SAFE** or **AT-RISK**.
- If AT-RISK: list each risky model + column + why (with the enforced-contract declared type or the type-dependent SQL).
- A short table of the columns actually consumed by eda-dbt-em (model → column → how used → physical prod type).
- State clearly whether eda-dbt-em consumes base's Salesforce/Google-Sheets models at all.
Be concise and evidence-based. Do not assume — cite files/lines and query results.
</user_query>

## 2. assistant

I'll start by setting up the repository and beginning the audit process.

## 3. assistant

I need to check the sources.yml file to see what pipeline eda-dbt-em is pulling from.

## 4. assistant

I'm noticing that eda-dbt-em pulls exclusively from `BASE_PROD.REDSHIFT_HISTORY` through a separate pipeline with SCD2 tables, which means it's not actually using the Salesforce or Google Sheets base models that PR #491 modifies. I should verify there aren't any other dependencies or cross-project references I'm missing.

## 5. assistant

I should look for cross-project references in eda-dbt-em that point to eda_dbt_base models, since it does have those dependencies, and also check what's in the other sources.yml files.

## 6. assistant

I'm seeing that eda-dbt-em actually does pull from eda_dbt_base models, particularly Salesforce and Google Sheets sources. The main Salesforce consumers include the Apttus agreement object with its custom columns, dated conversion rates, and opportunity data.

Now I need to search the em repo for all 52 changed column tokens to see which ones are actually being referenced in the SQL and YAML files. I'll build a regex pattern from the token list and grep through the codebase to identify the real exposures.

## 7. assistant

I'm checking the actual line-level matches for these tokens to verify the counts and rule out false positives like `wam` appearing inside other words, while also confirming that word boundaries are working correctly for patterns like `rate__c` versus `conversionrate__c`.

## 8. assistant

I'm filtering through these column references to identify which ones are actually meaningful. The `wam` hit is just descriptive text, not a real column reference, so I can discard it. For the actual matches, I need to understand that the source objects being modified in PR #491 are the base Salesforce and Google Sheets passthrough models, and the columns in question like `cash_upfront__c`, `exit_year_acv__c`, and `planning_acv__c` are the ones I need to track through the dependency chain.

Looking at `stg_em_opportunity_as_was.sql`, I can see these columns are being created as output aliases from source columns with different names — `opportunity_cash_upfront` becomes `cash_upfront__c`, `exit_year_acv_lc` becomes `exit_year_acv__c`, and so on. These source columns are coming from a unified history table with the `_lc` suffix and `opportunity_*` prefix, not directly from the changed Salesforce base columns. The key insight is that while the base Salesforce object has columns named `cash_upfront__c`, this staging model is reading from a different source (`s`, which appears to be an SCD2 table) and creating output columns with those same names. I need to trace back where `s` is actually sourced from to understand the full dependency.

In the downstream models like `stg_em_int_opp_base.sql`, these output columns are being read again — `cash_upfront__c` and `exit_year_acv__c` are pulled from the opportunity staging model, which means they're ultimately deriving from the unified history REDSHIFT tables, not the Salesforce base. For agreement lines, I'm seeing the same pattern: `total_workers_by_sku__c` originates from the REDSHIFT unified history in `wd_agreement_line_scd2.sql`, gets aliased in the staging layer, and then flows downstream through the integration models.

The key insight is that while eda-dbt-em declares columns with names matching the Salesforce fields that PR #491 changed, those columns are physically sourced from the REDSHIFT_HISTORY pipeline — a completely separate data flow. The column names coincide because they're Salesforce field names replicated into REDSHIFT, but the actual data bytes come from REDSHIFT, untouched by any YAML changes in eda-dbt-base.

However, eda-dbt-em does have direct dependencies on some eda-dbt-base Salesforce base models — like `base_salesforce_apttus__apts_agreement__c` in the agreement model, `base_salesforce_opportunity` in the opportunity staging, and `base_salesforce_datedconversionrate` — so those direct joins could be affected by the column changes.

I need to examine which of the 52 changed columns are actually selected in those directly-consumed base models to determine if there's real exposure.

## 9. assistant

I'm seeing that the staging table pulls its primary data from the GTM project's opportunity SCD2 table, and all those custom field aliases are actually just references to the source opportunity columns. The base Salesforce opportunity table is only being used for something specific, though the thought cuts off before revealing what.

Now I need to check the agreement staging table to see how it handles the Apttus agreement object with all those changed custom columns.

## 10. assistant

I'm checking which columns from the Apttus agreement table are actually being selected in the wd_agreement.sql file, and it looks like there's a mismatch between the 52 changed columns in the schema and what the query is actually pulling. I need to verify that the staging models are pulling from Redshift and check whether contract enforcement is enabled on those models.

## 11. assistant

I'm finding that enforced contracts aren't being used anywhere in the eda-dbt-em models, which rules out one potential breakage scenario. Now I'm looking at three specific staging models—stg_em_agreement_scd2, stg_em_agreement_line_item_scd2, and stg_em_opportunities_services_partner_junction_scd2—that all pull from a unified history source.

The key insight is that these models source from a separate REDSHIFT pipeline, not from eda-dbt-base's Salesforce models, so the column declarations in their YML files are for REDSHIFT-sourced columns and wouldn't be affected by the PR changes. I'm now checking whether the changed columns are being read through arithmetic or casting operations from the directly-consumed base models like base_salesforce_apttus__apts_agreement__c, base_salesforce_opportunity, base_salesforce_datedconversionrate, and base_workday_currency. So far, the directly-consumed models appear to select only specific columns that don't overlap with the 52 changed columns, but I need to verify this across all the base models. Cash_upfront__c in that context is being read from the as_was opportunity output, which sources from GTM, not from the changed base models. So that's also irrelevant to the risk assessment.

The core finding is that eda-dbt-em has no enforced contracts and doesn't directly consume any of the 52 changed columns from eda-dbt-base's Salesforce objects — they all flow through REDSHIFT pipelines instead. This means the PR poses no risk to eda-dbt-em.

Now I need to verify this with physical type evidence from Snowflake. Let me query the finance production database to check the actual column types for a few of the consumed columns, starting with identifying which database the wd_agreement and staging models land in.

## 12. assistant

I'm looking at the database naming convention—there's a `finance` and `finance_int` database with a prod prefix applied, so I need to check how the `generate_database_name` macro handles the naming pattern to understand if it produces something like `FINANCE_PROD` or `FINANCE_INT_PROD`.

## 13. assistant

I'm querying Snowflake through MCP to get the physical column types for the changed columns across the production databases, starting with what's actually being consumed from the changed base set. Now I'll query the information_schema in Snowflake to retrieve the actual physical column types for these tables.

## 14. assistant

I have all the evidence I need. Let me compile the verdict.

The physical column types in eda-dbt-em's production environment come from the REDSHIFT_HISTORY and GTM pipeline, along with the em repo's own casting logic rather than from eda-dbt-base. I'm noticing that the staging models already have some columns typed as FLOAT or BOOLEAN, like the INTEGRATE_TO_WORKDA column.

Looking at the source of truth, eda-dbt-em doesn't read these types from base — the scd2 staging models source directly from REDSHIFT_HISTORY, and the em repo handles the casting itself through its own macro. I'm verifying that wd_agreement_line_scd2 reads from the unified history base table (which is REDSHIFT via the base repo's passthrough), not from the SFDC or GSheets models that PR #491 changed, so there's no conflict there.

Now I'm mapping out the actual exposure: eda-dbt-em does consume several eda-dbt-base SFDC and GSheets models directly — the Apttus agreement, opportunity, dated conversion rate, Workday currency, and various Google Sheets reference models. But when I grep through all 52 changed column tokens, none of them appear in these reads; the references resolve to REDSHIFT-sourced or GTM columns instead. Let me do one final check to confirm this. Let me verify those other fields didn't get picked up anywhere — `ref_type`, `visit_search_engine`, `post_search_engine`, and `wam` all came back empty or were false positives, so none of them are actually being used. That confirms my findings are complete.

## 15. assistant

# Downstream Impact Audit — eda-dbt-em vs eda-dbt-base PR #491

## Verdict: **SAFE**

eda-dbt-em is not at risk from PR #491's YAML `data_type` changes. This holds for two independent reasons, each sufficient on its own.

---

## Why (evidence-based)

### 1. No enforced contracts anywhere in eda-dbt-em → breakage path (a) is impossible
```
rg "contract" -g '*.yml' -g '*.yaml' models | rg -i "enforce"  →  (zero matches)
```
There is not a single `contract: enforced: true` in the entire `models/` tree. A YAML `data_type` conflict cannot propagate here because no eda-dbt-em model re-declares a contracted type.

### 2. The 52 columns that eda-dbt-em references do NOT come from base's changed SFDC/GSheets models → breakage path (b) is not triggered
I searched all 52 tokens (word-boundary, case-insensitive) across every `.sql`/`.yml`/`.yaml` under `models/`. 36 of 52 tokens have **zero** hits. The 16 that appear all resolve to the **REDSHIFT_HISTORY pipeline or GTM project**, or are **output aliases** — never a read from an eda-dbt-base Salesforce/Google-Sheets model:

- `stg_em_agreement_scd2`, `stg_em_agreement_line_item_scd2`, `stg_em_opportunities_services_partner_junction_scd2` source from `source('base_prod','UNIFIED_HISTORY_*_SCD2')` = `BASE_PROD.REDSHIFT_HISTORY.*` (see `models/sources.yml` L6, and `source()` calls in each `.sql`). The columns `EDUCATION_TCV__C`, `RATE__C`, `TOTAL_WORKERS_BY_SKU__C`, `TOTAL_LIST_PRICE__C`, `APTS_*`, etc. are cast **inside em** via `generate_casted_columns`, from REDSHIFT bytes — not from base's Salesforce object.
- `stg_em_opportunity_as_was.sql` L147/41/101/102: `cash_upfront__c`, `exit_year_acv__c`, `planning_acv__c`, `prism_analytics_acv__c` are **output aliases** of `s.opportunity_*` columns where `s = ref('eda_dbt_gtm','wd_opportunity_scd2')` (L173). The only thing read from `base_salesforce_opportunity` (`b`, L174) is `b.fiscalyear` (L144) — not in the 52.
- `wam` is a false positive (the string "WAM" in a column *description*).

### 3. eda-dbt-em DOES consume base's Salesforce/Google-Sheets models — but only for out-of-scope columns
This is a real cross-project dependency (`dependencies.yml` lists `eda_dbt_base`), so it deserved the check. Confirmed direct reads of base SFDC/GSheets passthrough models:

| eda-dbt-em model | base model consumed | changed columns read? |
|---|---|---|
| `wd_agreement.sql` | `base_salesforce_apttus__apts_agreement__c` | No — selects only agreement-header fields (`legal_status__c`, `agreement_type__c`, `apttus__status__c`, `apts_term_*`, …); none of the 52 |
| `stg_em_opportunity_as_was.sql` | `base_salesforce_opportunity` | No — reads only `b.fiscalyear` |
| `stg_em_datedconversionrate.sql` | `base_salesforce_datedconversionrate` | No |
| `stg_em_ref_fx_conversion.sql` | `base_workday_currency` | No |
| ~20 `stg_em_vw_ref_*` / lookup models | `base_google_sheets_ref_*` | No — geo/industry/product-hierarchy/SSR/strategic-partner reference sheets; none of the 52 tokens |

So: **eda-dbt-em consumes base's Salesforce and Google-Sheets models — but never reads any of the 52 columns PR #491 changed.**

---

## Columns actually materialized by eda-dbt-em (physical prod types, `FINANCE_INT_PROD.information_schema`)

These share names with the 52 but are produced by em's own casting off REDSHIFT/GTM, independent of base YAML:

| Model (schema STAGE) | Column | How used | Physical prod type |
|---|---|---|---|
| STG_EM_OPPORTUNITY_AS_WAS | CASH_UPFRONT__C | alias of GTM `opportunity_cash_upfront` | FLOAT |
| STG_EM_OPPORTUNITY_AS_WAS | EXIT_YEAR_ACV__C | alias of GTM `exit_year_acv_lc` | NUMBER(35,17) |
| STG_EM_OPPORTUNITY_AS_WAS | PLANNING_ACV__C | alias of GTM `opportunity_planning_acv_lc` | NUMBER(35,17) |
| STG_EM_OPPORTUNITY_AS_WAS | PRISM_ANALYTICS_ACV__C | alias of GTM `opportunity_prism_analytics_acv_lc` | NUMBER(35,17) |
| STG_EM_AGREEMENT_SCD2 | EDUCATION_TCV__C | cast from REDSHIFT unified history | NUMBER(35,17) |
| STG_EM_AGREEMENT_LINE_ITEM_SCD2 | RATE__C | cast from REDSHIFT | FLOAT |
| STG_EM_AGREEMENT_LINE_ITEM_SCD2 | TOTAL_WORKERS_BY_SKU__C | select/passthrough (also `wd_agreement_line_scd2`) | FLOAT |
| STG_EM_AGREEMENT_LINE_ITEM_SCD2 | TOTAL_LIST_PRICE__C | cast from REDSHIFT | NUMBER(35,17) |
| STG_EM_AGREEMENT_LINE_ITEM_SCD2 | PRO_PKG_TIER_PRICE__C | cast from REDSHIFT | FLOAT |
| STG_EM_AGREEMENT_LINE_ITEM_SCD2 | APTS_SOURCING_QUANTITY__C | cast from REDSHIFT | FLOAT |
| STG_EM_AGREEMENT_LINE_ITEM_SCD2 | SALES_REPORTING_SKU__C | cast from REDSHIFT | BOOLEAN |
| STG_EM_AGREEMENT_LINE_ITEM_SCD2 | INTEGRATE_TO_WORKDAY__C | cast from REDSHIFT | BOOLEAN |
| STG_EM_AGREEMENT_LINE_ITEM_SCD2 | APTS_REPORTING_HISTORY_MONTH_PRICE__C | cast from REDSHIFT | NUMBER(38,17) |
| STG_EM_AGREEMENT_LINE_ITEM_SCD2 | APTS_TOTAL_REPORTING_MONTHS__C | cast from REDSHIFT | NUMBER(2,0) |
| STG_EM_AGREEMENT_LINE_ITEM_SCD2 | APTS_ACCOUNTING_NET_PRICE__C | cast from REDSHIFT | NUMBER(38,17) |

Note these em prod types (BOOLEAN, FLOAT, NUMBER) are set by em's own DDL from the REDSHIFT source; changing a `data_type` string in base's Salesforce YAML cannot alter them.

## Does eda-dbt-em consume base's Salesforce / Google-Sheets models?
**Yes** — it directly refs `base_salesforce_*` (apttus agreement, opportunity, datedconversionrate) and ~20 `base_google_sheets_ref_*` lookup models. **But none of those reads touch any of the 52 changed columns**, so the exposure is nil.

## Compile-level e2e check — SKIPPED (intentionally)
I did not run `dbt compile`/`parse`. The affected models use cross-project `ref('eda_dbt_base', …)` (and gtm/common/cx), which require the sibling project manifests plus warehouse/SSO credentials to resolve in a fresh `/tmp` clone — that would risk an interactive SSO browser hang, which the task explicitly says to avoid. The static analysis (no enforced contracts; sources traced to REDSHIFT_HISTORY/GTM) plus the live Snowflake physical-type check already establish safety without it.
