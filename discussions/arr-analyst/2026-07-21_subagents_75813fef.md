# Chat — Explore the standalone Python application for the "FQC-ARR" (Finance ARR Quarter…

- **Agent bucket:** `arr-analyst`
- **Cursor project:** `Users-koteswararao-venkata-Documents-Cursor-eda-dbt-em`
- **Transcript id:** `subagents`
- **Last updated:** 2026-07-21T10:00:02
- **Turns kept:** 5 (user/assistant text only; tool payloads omitted)

---

## 1. user

<timestamp>Tuesday, Jul 21, 2026, 9:56 AM (UTC-7)</timestamp>
<user_query>
Explore the standalone Python application for the "FQC-ARR" (Finance ARR Quarter Close) agent. It lives on this Mac at:

- Primary: `/Users/koteswararao.venkata/Library/Application Support/AE Agent/`
- Also possibly a package dir named `agents/arr_quarter_close/` somewhere under that path (module path `agents.arr_quarter_close.supervisor`), and re-seed/dist copies at `/Users/koteswararao.venkata/Documents/Cursor/fqc-arr-dist/AE Agent/`.

Be "very thorough". I need a precise architecture map so I can port this agent to another platform. Report back with:

1. **Directory tree** (2-3 levels deep) of the AE Agent home and the `agents/arr_quarter_close/` package.
2. **Supervisor** (`supervisor.py`): the orchestration loop, the DAG definition (`_role_dag()`), how it dispatches each role, the `SupervisorInput` / `SupervisorState` / `SupervisorRunReport` dataclasses, auth modes, pause/resume logic, and how Slack + thinking-log are wired in.
3. **Sub-agents** (`subagents/*.py`): list every module, and for each: its Input/Output dataclasses, the `plan()` and `run()` signatures, what external tools it calls (Jira curl, Snowflake MCP, dbt, gh CLI, Slack `slk`/`node`), and its `RoleResult` payload keys.
4. **Shared contracts/models**: any `models.py` / `contracts.py` / `types.py` — the `RoleResult`, `RoleStatus`, `ValidationMatrix`, `ValidationCheck`, `TicketSpec` definitions.
5. **CLI** (`cli.py`): all flags and how they map onto `SupervisorInput`.
6. **State / persistence**: the lessons store (JSONL — where, schema, how lessons are loaded/added/promoted/archived, `_audit_verifier.py`, `_audit_apply.py`), the `runs/` folder (thinking, backups, learning, pr_mining, audit), `slack_directory.json`, and any launchd plist files (weekly backup, lesson-audit).
7. **External integrations & credentials**: how it reads env vars (Jira, Snowflake, dbt, Slack, GitHub, Sigma), and any hardcoded assumptions (paths, warehouse names, MCP usage).
8. **Requirements / dependencies**: any `requirements.txt` / `pyproject.toml` / imports of third-party libs.
9. **LLM usage**: where and how it calls an LLM (model names like `claude-opus-4-7-thinking-xhigh`), and where prompts are stored.

Do NOT modify any files — read only. Return a structured markdown report with file paths and concrete function/class names, plus code snippets for the supervisor loop and the RoleResult/ValidationMatrix contracts.
</user_query>

## 2. assistant

I'll map the FQC-ARR agent architecture from the AE Agent home and related package paths—starting with the directory layout, then drilling into supervisor, sub-agents, and contracts.

## 3. assistant

Next I'll read the supervisor, contracts, CLI, and shared core modules, then inventory every sub-agent.

## 4. assistant

Pulling the supervisor orchestration pieces and scanning every sub-agent for contracts and tool calls.

## 5. assistant

# FQC-ARR Architecture Map (porting reference)

**Primary home:** `/Users/koteswararao.venkata/Library/Application Support/AE Agent/`  
**Symlink (UI):** `~/Documents/Cursor/AE Agent` → same path  
**Dist/re-seed:** `/Users/koteswararao.venkata/Documents/Cursor/fqc-arr-dist/AE Agent/` (code mirror; no live `runs/`)  
**Import root:** `agents.arr_quarter_close.*` via `PYTHONPATH=$AE_AGENT_HOME` from `bin/fqc-arr`  
**dbt project (separate):** `$FQC_ARR_PROJECT_DIR` → typically `~/Documents/Cursor/eda-dbt-em`

---

## 1. Directory trees

### AE Agent home (2–3 levels)

```
AE Agent/
├── README.md
├── agents/
│   └── arr_quarter_close/          # full Python package
├── bin/
│   ├── fqc-arr                     # launcher
│   ├── fqcarr_learn.sh             # twice-daily learning
│   ├── fqcarr_backup_weekly.sh
│   ├── fqcarr_lesson_audit.sh
│   ├── ae-do-ticket, ae-ship-pr
│   ├── watch_dbt_run.py, watch_pr_ci.py/.sh
├── launchd/
│   ├── com.kvenkata.fqcarr.learn-am.plist      # 09:00
│   ├── com.kvenkata.fqcarr.learn-pm.plist      # 17:00
│   └── com.kvenkata.fqcarr.backup-weekly.plist # Sun 02:00
└── runs/
    ├── thinking/     # live Markdown traces
    ├── learning/     # --learn audit reports
    ├── audit/        # lesson_audit_*.json
    ├── backups/      # weekly backup notes
    └── pr_mining/    # mined PR history (details/, diffs/)
```

### `agents/arr_quarter_close/` package

```
arr_quarter_close/
├── supervisor.py, cli.py, contracts.py, core.py
├── lessons.py, notifier.py, thinking_log.py
├── cursor_runner.py, sana_adapter.py (stub)
├── slack_directory.py, requirements.txt, README.md
├── subagents/          # 10 DAG + on-demand workers
│   ├── jira_intake.py … qa_handoff.py
│   ├── debugger.py, quarter_close_runner.py, daily_reflection.py
│   └── _validation_matrix.py
└── data/
    ├── slack_directory.json
    ├── seed_*.py, _scrape_eda_channel.py
    └── lessons/        # JSONL store + audit/mining scripts
```

---

## 2. Supervisor (`supervisor.py`)

### Identity
- `SUPERVISOR_DISPLAY_NAME = "Finance ARR Quarter Close (FQC-ARR)"`
- `SUPERVISOR_SHORT_CODE = "FQC-ARR"`
- Aliases: `FQC-ARR`, `FQCARR`, `FQC`, …

### Dataclasses

| Type | Role |
|------|------|
| `SupervisorInput` | Drive one run (ticket, as_was_date, auth, Slack, skip, debug, quarter-close, reflect, lessons flags) |
| `SupervisorState` | Intermediate payloads between roles / resume calls |
| `SupervisorRunReport` | Final envelope (`contracts.py`) |

`SupervisorInput` key fields: `project_dir`, `ticket_key`, `as_was_date`, `auth_mode`, `slack_channel`, `slack_notify`, PR/CD overrides, `dry_run`, `skip_roles`, thinking-log flags, `debug_*`, clarifier timeouts, `quarter_close_*`, `notify` → resolved `notify_user_ids`, `auto_reflect` / `reflect_only` / `inject_lessons`.

`SupervisorState` payload slots absorbed by `_absorb()`:
`ticket`, `requirements`, `validation`, `implementation`, `test_report`, `pr`, `ci_report`, `cd_report` (+ `side_tasks`, `last_side_channel_ts`).

### Modes (`resolve_mode` / `run`)
1. **ticket** — 10-role DAG  
2. **scheduled** — `ARRCloseOrchestrator` as `scheduled-close`, or `quarter-close-runner` if `--quarter-close`  
3. **both** — ticket DAG, then optional inline scheduled rebuild if implementer sets `needs_snapshot_rebuild`  
4. **reflect_only** — skip DAG; only `daily-reflection`

### DAG (`_role_dag`)

```585:597:/Users/koteswararao.venkata/Library/Application Support/AE Agent/agents/arr_quarter_close/supervisor.py
    def _role_dag(self) -> list[tuple[str, callable]]:
        return [
            ("jira-intake",            self._run_jira_intake),
            ("requirements-analyzer",  self._run_requirements_analyzer),
            ("code-data-validator",    self._run_code_data_validator),
            ("clarifier",              self._run_clarifier),
            ("implementer",            self._run_implementer),
            ("test-runner",            self._run_test_runner),
            ("pr-author",              self._run_pr_author),
            ("ci-monitor",             self._run_ci_monitor),
            ("cd-monitor",             self._run_cd_monitor),
            ("qa-handoff",             self._run_qa_handoff),
        ]
```

On-demand (not in DAG): `debugger`, `quarter-close-runner`, `daily-reflection`.

### Orchestration loop (ticket mode)

```431:573:/Users/koteswararao.venkata/Library/Application Support/AE Agent/agents/arr_quarter_close/supervisor.py
    def _run_ticket_mode(self, started: float) -> SupervisorRunReport:
        roles = self._role_dag()
        for role_name, fn in roles:
            # --skip → SKIPPED; --dry-run → plan_only=True
            # else fn(plan_only=False) → RoleResult
            # FAIL → _maybe_debug_on_failure → _pause
            # NEEDS_INPUT (clarifier) → stdin ans / Slack `ans:` then continue or _pause
            # _drain_side_tasks() for `task:` (skip/pause/cancel/status/debug/quarter-close)
        ...
```

Dispatch pattern: each `_run_<role>` builds a typed `*Input` from state stubs, calls `subagent.plan(req)` or `subagent.run(req)`.

### Auth modes (`contracts.AuthMode`)
| Mode | Behavior in practice |
|------|----------------------|
| `FULL_AUTO` (`--auto`) | Clarifier/PR/QA/debugger may write (Jira/gh) |
| `SMART_GATES` (default) | Pause before writes (`NEEDS_INPUT`) |
| `GATED_MINIMAL` / `GATED_FULL` | Enumerated; **treated like smart_gates** (`!= FULL_AUTO` → pause) — no finer differentiation in code |

### Pause / resume
- Pause: `_pause(result)` → `SupervisorRunReport` with `pause_points=[{role, reason}]` and `overall_status` = failing/needs_input status.
- **No `Supervisor.resume()` method exists** despite docstring mention. Resume = new call:
  `Supervisor(SupervisorInput(...), state=prior_SupervisorState).run()`  
  with state payloads + CLI overrides (`pr_url_override`, `skip_roles`, etc.).

### Slack + thinking-log wiring
- **Slack:** `SlackNotifier` (`notifier.py`) if not dry-run and `slack_notify` and `slack_channel` set. Parent via `slk send`; threads via `node` + `/opt/homebrew/lib/node_modules/slkcli/src/api.js`. Banners on start/finish; per-role `post_role_result`; side-channel poll for `task:` / clarifier `ans:`.
- **Thinking log:** `ThinkingLog` → default `<project_dir>/runs/thinking/<UTC>_<slug>.md` (`default_thinking_log_path`). Header / role_start / role_end / side_task / orchestrator_step / footer. Fail-safe (IO errors disable writer).

---

## 3. Sub-agents (`subagents/*.py`)

Every module exposes `ROLE`, `plan(req) -> dict`, `run(req) -> RoleResult`.

| Module | Input | Output shape / payload keys | Tools | Notes |
|--------|-------|-----------------------------|-------|-------|
| `jira_intake` | `TicketInput` | `{"ticket": TicketSpec}` | `curl` + `JIRA_*` | ADF flatten, AC extract |
| `requirements_analyzer` | `RequirementsInput` | `requirements`, `prompt`, `preferred_model` | none (heuristic + prompt) | Usually `NEEDS_INPUT` for LLM |
| `code_data_validator` | `ValidationInput` | `{"validation": ValidationReport}` | `rg`/`grep`; SQL templates for Snowflake MCP | Never opens Snowflake |
| `clarifier` | `ClarificationInput` | `clarification` (+ `preferred_model` when gated) | `curl` Jira POST if FULL_AUTO | Pause unless FULL_AUTO |
| `implementer` | `ImplementationInput` | `implementation`, `prompt`, `branch_*`, `preferred_model` | `git checkout -b` | Always `NEEDS_INPUT` (LLM edits) |
| `test_runner` | `TestInput` | `{"test_report": TestReport}` + artifacts | `pytest`, `dbt test` | Matrix in report |
| `pr_author` | `PRInput` | `{"pr": PRResult}` (+ title/body when gated) | `git push`, `gh pr create` | Pause unless FULL_AUTO |
| `ci_monitor` | `CIInput` | `{"ci_report": CIReport}` | `gh pr checks`, `slk`; SQL notes for MCP | Poll loop |
| `cd_monitor` | `CDInput` | `{"cd_report": CDReport}` | dbt Cloud `curl` / optional `monitor_dbt_run.py`, `slk` | Needs run id |
| `qa_handoff` | `QAHandoffInput` | `qa_handoff` (+ `adf_preview` when gated) | `curl` Jira comment/attach | Pause unless FULL_AUTO |
| `debugger` | `DebugInput` | `{"debug": DebugReport}` | ripgrep lineage, write pytest/dbt harness, optional Jira | On FAIL / `--debug` / `task: debug` |
| `quarter_close_runner` | `QuarterCloseInput` | `{"quarter_close": QuarterCloseReport}` | `ARRCloseOrchestrator` → `dbt`; recon SQL for MCP | On-demand |
| `daily_reflection` | `ReflectionInput` | `{"reflection": ReflectionReport}` | filesystem scan + `LessonRecorder` | End-of-run / `--learn` |
| `_validation_matrix` | helpers | `ValidationMatrix` of 4 standard checks | SQL templates only | Shared by validator/CI/CD |

---

## 4. Shared contracts (`contracts.py`)

No separate `models.py` / `types.py` — all contracts live here.

### `RoleResult` / `RoleStatus`

```76:103:/Users/koteswararao.venkata/Library/Application Support/AE Agent/agents/arr_quarter_close/contracts.py
class RoleStatus(str, Enum):
    OK = "ok"
    NEEDS_INPUT = "needs_input"
    WARN = "warn"
    FAIL = "fail"
    SKIPPED = "skipped"

@dataclass
class RoleResult:
    role: str
    status: RoleStatus
    summary: str
    payload: dict[str, Any] = field(default_factory=dict)
    artifacts: dict[str, str] = field(default_factory=dict)
    pause_reason: Optional[str] = None
    preferred_model: Optional[str] = None
```

### `ValidationCheck` / `ValidationMatrix`

```203:281:/Users/koteswararao.venkata/Library/Application Support/AE Agent/agents/arr_quarter_close/contracts.py
@dataclass
class ValidationCheck:
    check_name: str
    grain: str = ""
    source_salesforce: str = ""
    baseline_prod: str = ""
    target_dev_qa: str = ""
    expected: str = ""
    actual: str = ""
    business_logic: str = ""
    verdict: str = "pending"
    variance_abs: Optional[float] = None
    variance_pct: Optional[float] = None
    notes: str = ""
    sql_template: str = ""

@dataclass
class ValidationMatrix:
    matrix_name: str
    target_db: str
    baseline_db: str = "finance_prod"
    source_db: str = "base_prod.salesforce"
    checks: list[ValidationCheck] = field(default_factory=list)
    overall_verdict: str = "pending"  # derived in __post_init__ / recompute_verdict()
```

### `TicketSpec` (and related)
`TicketSpec`: `ticket_key`, `summary`, `status`, `assignee`, `reporter`, `issue_type`, `labels`, `components`, `description_text`, `acceptance_criteria`, `comments`, `raw_url`.

Also defined: `RequirementsSpec`/`KPISpec`, `ValidationReport`, `ClarificationRequest`, `ImplementationResult`, `TestReport`, `PRResult`, `CIReport`/`CDReport`, `QAHandoffResult`, full debugger/quarter-close/reflection types, `SideTask`, `SupervisorRunReport`, `AuthMode`, `DEFAULT_LLM_MODEL`.

---

## 5. CLI (`cli.py`) → `SupervisorInput`

Entrypoint: `python -m agents.arr_quarter_close.cli` via `bin/fqc-arr`.

| Flag | Maps to |
|------|---------|
| `--ticket` | `ticket_key` |
| `--as-was-date` | `as_was_date` (scheduled / both) |
| `--mode {auto,scheduled,ticket}` | routing |
| `--auth-mode` / `--auto` | `auth_mode` (`FULL_AUTO` if `--auto`) |
| `--model` | sets `$FQC_ARR_DEFAULT_MODEL` |
| `--slack-channel` / `--no-slack` | `slack_channel` / `slack_notify` |
| `--skip` (repeat) | `skip_roles` |
| `--pr-number` / `--pr-url` / `--check-name` / `--dbt-cloud-run-id` | overrides |
| `--notify` | `notify` → Slack directory resolve |
| `--thinking-log` / `--no-thinking-log` | thinking log path/enable |
| `--debug-model` / `--debug` / `--no-debug-on-failure` | debugger |
| `--clarifier-interactive-timeout` / `--no-clarifier-interactive` | stdin wait |
| `--clarifier-slack-timeout` / `--no-clarifier-slack` | Slack `ans:` wait |
| `--quarter-close*` | quarter-close-runner options |
| `--reflect` / `--learn` / `--reflect-*` / `--no-auto-reflect` | reflection |
| `--show-lessons` | exit early; dump lessons |
| `--no-inject-lessons` / `--max-lessons-per-role` | lesson injection |
| `--project-dir`, `--profiles-dir`, `--target`, `--heavy-warehouse` | dbt (scheduled path) |
| `--refresh-dashboards`, `--no-validation`, `--no-ia-migration-tests` | orchestrator |
| `--dry-run`, `--no-fail-fast`, `--json`, `-v` | plan/output |

`bin/fqc-arr` also sets `AE_AGENT_HOME`, `PYTHONPATH`, default `FQC_ARR_LESSONS_DIR`, and resolves dbt project: CLI flag → `$FQC_ARR_PROJECT_DIR` → CWD with `dbt_project.yml` → `~/Documents/Cursor/eda-dbt-em`.

---

## 6. State / persistence

### Lessons (`lessons.py` + `data/lessons/`)
- **Location:** `$FQC_ARR_LESSONS_DIR` or `<project_dir>/agents/arr_quarter_close/data/lessons/` (launcher pins agent-home path).
- **Files:** `<role>.jsonl`, `_global.jsonl`, `_stable.jsonl` (promoted), `_reflection_log.jsonl`.
- **Schema (`Lesson`):** `id` (sha1 of role+lesson[:12]), `role`, `category`, `lesson`, `evidence`, `tags`, `confidence`, `occurrence_count`, `status` (`active`|`promoted`|`deprecated`; audit also uses `archived`), `source_ticket`, `source_run_ts`, `first_seen`, `last_seen`.
- **API:** `LessonRecorder.record()` (dedupe + bump; promote to `_stable` at `occurrence_count >= 3`), `load_for()`, `log_reflection()`, `reflected_today()`, `get_recorder()`.
- **`ARCHIVE_AFTER_DAYS = 90`** is defined but **not auto-enforced** in recorder; archival is via `_audit_apply.py` / `_correction_run_*.py` setting `status=archived`.
- **Audit:** `_audit_verifier.py` → `runs/audit/lesson_audit_*.json`; `_audit_apply.py` applies corrections. Driven by `bin/fqcarr_lesson_audit.sh` (uses bare clone `~/Backups/eda-dbt-em-backup.git`).

### `runs/`
| Dir | Purpose |
|-----|---------|
| `thinking/` | Live Markdown (also under dbt `project_dir/runs/thinking` by default) |
| `learning/` | `--learn` reports + launchd stdout |
| `audit/` | Lesson verification JSON |
| `backups/` | Weekly backup logs |
| `pr_mining/` | Historical PR mining artifacts |

### `slack_directory.json`
Keys: `users`, `aliases`, `channels`, `version`, `updated_at`. ~22 users / 38 aliases. Resolved by `SlackDirectory.resolve()` for `--notify`.

### launchd
| Plist | Schedule | Program |
|-------|----------|---------|
| `com.kvenkata.fqcarr.learn-am` | 09:00 | `fqcarr_learn.sh` → `fqc-arr --learn` |
| `com.kvenkata.fqcarr.learn-pm` | 17:00 | same |
| `com.kvenkata.fqcarr.backup-weekly` | Sun 02:00 | `fqcarr_backup_weekly.sh` |

Active copies expected under `~/Library/LaunchAgents/`; mirrors under agent `launchd/`.

---

## 7. External integrations & credentials

| System | How | Env / assumptions |
|--------|-----|-------------------|
| **Jira** | `curl` Basic Auth | `JIRA_BASE_URL` (default `https://workdaybt.atlassian.net`), `JIRA_EMAIL`, `JIRA_API_TOKEN` |
| **Snowflake** | **Not connected in-process** — SQL templates for **Snowflake MCP** (caller/IDE) | DBs: `finance_dev`/`finance_qa`/`finance_prod`, `certified_dev` (quarter-close target default), `base_prod.salesforce` |
| **dbt** | local `dbt` CLI via `ARRCloseOrchestrator` / test-runner | `$FQC_ARR_PROJECT_DIR`; `em_heavy_warehouse` var; exclude `*_scd2` |
| **dbt Cloud** | `curl` API / optional script | `DBT_CLOUD_BASE_URL`, `DBT_CLOUD_ACCOUNT_ID`, `DBT_CLOUD_API_TOKEN` |
| **GitHub** | `gh`, `git` | Auth via `gh` session |
| **Slack** | `slk` + `node`/slkcli | Channel/user id; `FQC_LEARN_SLACK_USER`; hardcoded slkcli path under Homebrew |
| **Cursor SDK** | optional `cursor_runner.py` | `CURSOR_API_KEY`; optional `cursor-sdk` |
| **Sigma / Salesforce** | not called by this package | — |
| **LLM** | **no API call** — emit `prompt` + `preferred_model` | `FQC_ARR_DEFAULT_MODEL` |

Hardcoded paths of note: agent under Application Support; dbt fallback `~/Documents/Cursor/eda-dbt-em`; slkcli at `/opt/homebrew/lib/node_modules/slkcli/src/api.js`; audit bare repo `~/Backups/eda-dbt-em-backup.git`.

---

## 8. Requirements / dependencies

`requirements.txt`: **stdlib-only** for core path. Optional commented `cursor-sdk>=0.5.0`.

**External CLIs expected on PATH:** `python3`, `dbt`, `pytest`, `git`, `gh`, `curl`, `rg`/`grep`, `slk`, `node` (Slack threads).

No `pyproject.toml` in the agent package. Third-party Python imports are not required for supervisor/subagents (stdlib + subprocess).

---

## 9. LLM usage

**The supervisor and sub-agents do not call an LLM API.** They emit prompts and pause.

| Where | Mechanism |
|-------|-----------|
| Default model | `contracts.DEFAULT_LLM_MODEL = "claude-opus-4-7-thinking-xhigh"` |
| Override | `--model` > `$FQC_ARR_DEFAULT_MODEL` > constant |
| Allowed slugs (docs) | opus-4.7-xhigh, opus-4.8-high, 4.6-opus-high-thinking, gpt-5.5-medium, gpt-5.3-codex, gemini-3.1-pro |
| Prompt authors | `requirements_analyzer._build_llm_prompt`, `implementer._build_llm_prompt`; clarifier carries `preferred_model` at pause |
| Prompt storage | **Inline in those modules** (not separate prompt files) |
| Lessons injection | `format_lessons_for_prompt(recorder.load_for(role))` into prompts |
| Cursor SDK | `cursor_runner.py` — `_AGENT_PROMPT_TEMPLATE` + `CursorRunOptions.model`; uses Cursor agent for Mode A close |
| SANA | stub only (`sana_adapter.py`) |

Caller (Cursor chat / SDK / human) is expected to run the emitted prompt on `preferred_model`, then resume with updated `SupervisorState` payloads.

---

## Porting checklist (practical)

1. Copy package + pin `FQC_ARR_HOME` / `FQC_ARR_LESSONS_DIR` / `FQC_ARR_PROJECT_DIR`.  
2. Reimplement launcher (`PYTHONPATH` + project-dir resolution).  
3. Keep `contracts.py` + `Supervisor` DAG as the portable core.  
4. Replace Slack (`slk`/`node`) and Jira (`curl`) transports if the new platform differs.  
5. Supply an LLM runner that consumes `RoleResult.payload["prompt"]` + `preferred_model`.  
6. Keep Snowflake as MCP/SQL-execution side-effect outside Python.  
7. Port lessons JSONL + optional launchd/cron for `--learn`.  
8. Note auth: only `FULL_AUTO` vs “everything else pauses”; gated_minimal/full are placeholders.  
9. Resume is **state re-injection**, not a dedicated resume API.

**Dist vs live:** `fqc-arr-dist` mirrors code + seed lessons/slack_directory; live agent also has populated `runs/` and mining/audit scripts under `data/lessons/`.
