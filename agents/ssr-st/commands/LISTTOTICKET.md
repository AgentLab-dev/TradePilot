# Command: LIST TO TICKET

Trigger: `LIST TO TICKET`, `list-to-ticket`, `list to ticket`.

Skill: `agents/ssr-st/skills/list-to-ticket/SKILL.md`

Read live IBD lists + PPS-T7 ON + ALWAYS names. Every one gets **TICKET** or **SKIP (gate)** the same session. Fail if any listed name is only "board" / "watching". Wait for **go**.
