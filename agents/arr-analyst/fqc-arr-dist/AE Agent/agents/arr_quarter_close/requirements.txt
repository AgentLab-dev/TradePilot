# Core orchestration uses only the Python standard library and the local
# dbt CLI - no third-party install is required to run `core.py` / `cli.py`.
#
# Optional extras below enable the Cursor SDK runner and the (future) SANA
# adapter. Install them only when you need those runtimes.

# Cursor SDK runner (cursor_runner.py)
# cursor-sdk>=0.5.0  # uncomment when using run_close_via_prompt / run_close_via_core

# dbt itself is expected to be available on PATH and managed by the parent
# repo's tooling (pyproject / pip-tools / dbt Cloud).
