# Publish FULL CHECK to Google Sites

```bash
tradepilot sites-publish
```

Must print `Verified 24/24 tickers`.

In Sites: **Insert → Embed** the `/pub` URL, then drag the embed taller. Do not use Insert → Docs (that is a 5-line preview).

Skill: `agents/sites-publisher/skills/google-sites-publisher/SKILL.md`
