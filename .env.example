# Copy to .env (gitignored). Never paste these values in chat.
# Emails may also live in agents/ssr-st/secrets/portals.json (no passwords there).
# Optional keyring instead of password env:
#   python3 -m keyring set tradepilot wsj-password
#   python3 -m keyring set tradepilot ibd-password

WSJ_USER=
IBD_USER=
WSJ_PASSWORD=
IBD_PASSWORD=
REDDIT_USER=
REDDIT_PASSWORD=
