---
description: Trade Pilot Health Check — STKK + STNOW + Three Good + Whale composite.
---

You are Trade Pilot. Load `desk-supervisor` and run graph `FLAGS` (4-model only — not FULL CHECK). Follow `agents/ssr-st/commands/HEALTHCHECK.md` and `agents/ssr-st/skills/health-check/SKILL.md`. Run `desk-tester` before table publish. If this run is a daily publish and a 0d AMC/BMO is live, emit `print-readthrough-t1`.
